import arcgisSetting from "src/config/arcgis";

import minemapSetting from "src/config/minemap";

const setting = {
    mapType: "arcgis",
    arcgis: arcgisSetting,
    minemap: minemapSetting
};

export default setting;
